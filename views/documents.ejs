<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Documents</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="documents-container">
        <h1>My Documents</h1>
        <ul>
            <% documents.forEach(doc => { %>
                <li>
                    <a href="/staff/download-document/<%= doc.id %>" download><%= doc.filename %></a>
                    <span>(Uploaded on: <%= doc.uploaded_at %>)</span>
                </li>
            <% }) %>
        </ul>
    </div>
</body>
</html>
